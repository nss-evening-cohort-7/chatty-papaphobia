let messageData = [];

const getMessages = () => {
  return getMessages;
};

const setMessages = (messagesArray) => {
  messages = messagesArray;
};

module.exports = {
  getMessages,
  setMessages,
};
