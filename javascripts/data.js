let messageData = [];

const getMessages = () => {
  return messageData;
};

const setMessages = (messagesArray) => {
  messageData = messagesArray;
};

module.exports = {
  getMessages,
  setMessages,
};
